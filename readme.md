to start frist run main.py to creat embeddgins if you add a new doc/pdf
to start the project we use  
python -m uvicorn app:app --reload --port 8000
embeddings we are using sentence transfromer andf for llm we are using groq fr
